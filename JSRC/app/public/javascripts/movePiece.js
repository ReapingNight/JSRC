var yourTurn = true;
var firstSelect;
var secondSelect;
var selectIndex = 0;

function select()
{
    if(yourTurn)
    {
        switch (selectIndex%2)
        {
            case 0:
                
                break;
            case 1:

                break;
            default:
                break;
        }
    }
}