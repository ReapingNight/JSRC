function ChessPiece(Color, Type)
{
    this.color = Color;
    this.type = Type;
    this.position = boardPosition;
    chessfunction = validFunction(Color, Type);
}

function validFunction(Color, Type)
{
    


}
