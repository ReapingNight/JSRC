var game = new Object(
    id = 0,
    players = [],
    options = {},
    status = 0
)

function Game(id, players, options, status){
    this.id = id;
    this.players = players;
    this.options = options;
    this.status = status
}

function newGame(id, players, options, status)
{
    return new Game(id, players, options, status);
}

module.exports = {game: game, newGame: newGame};